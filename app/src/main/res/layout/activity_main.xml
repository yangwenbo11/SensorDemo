<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.administrator.sensordemo.MainActivity">

    <Button
        android:id="@+id/all_btn"
        android:onClick="getAllSensor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="获取手机中所有的传感器"/>

    <TextView
        android:id="@+id/sensor_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/all_btn"/>

    <Button
        android:id="@+id/accelerometer_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="加速度传感器"
        android:onClick="getAccelerometer"
        android:layout_below="@id/sensor_text"/>

    <Button
        android:id="@+id/proximity_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="邻近传感器"
        android:onClick="getProximity"
        android:layout_below="@id/accelerometer_btn"/>
</RelativeLayout>
